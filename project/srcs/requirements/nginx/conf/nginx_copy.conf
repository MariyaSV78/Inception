
events {
	worker_connections 1024;
}

http {

    server {

        listen      443 ssl;
        listen [::]:443 ssl;

        server_name msosnova.42.fr www.msosnova.42.fr localhost;

# Dossier contenant mon site web, à remplacer par root /var/www/wordpress dans le futur
        root    /var/www/wordpress;

        index index.php index.html index.htm;

        ssl_certificate     /etc/nginx/ssl/msosnova.42.fr.crt;
        ssl_certificate_key /etc/nginx/ssl/msosnova.42.fr.key;

        ssl_protocols       TLSv1.2 TLSv1.3;
    
        location / {
            try_files $uri $uri/ =404;
            autoindex on;
        }
        
    #     location ~ \.php$ { 
    # 	    # fastcgi_split_path_info ^(.+\.php)(/.+)$;
    #         fastcgi_pass wordpress:9000;
    #         fastcgi_index index.php;
    #         include fastcgi_params;
    #         fastcgi_param SCRIPT_FILENAME $document_root$fastcgi_script_name;
    #         fastcgi_param PATH_INFO $fastcgi_path_info;
    #   }

        # error_log /var/log/nginx/error.log;
        # error_page 500 502 503 504 /50x.htlm;

    }
}
